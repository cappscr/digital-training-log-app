# Digital Training Log App

This is the monorepo for the Digital Training Log App

## Frontend

In the `frontend` directory there is a React SPA.

## Backend

In the `backend` directory there is an api only Rails application.
