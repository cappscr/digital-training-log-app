export const SectionLabel = ({ label }: { label: string }) => {
  return (
    <span className="text-primary mb-4 block text-xs font-medium tracking-widest uppercase">
      {label}
    </span>
  );
};
